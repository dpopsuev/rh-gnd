apiVersion: origami/v1
kind: Schematic
metadata:
  name: origami-gnd
  module: github.com/dpopsuev/origami-gnd
  namespace: dsr
  description: "Gather & Diffuse — pluggable code context gathering and synthesis"
spec:
  version: "1.0.0"
  resolver: SchematicResolver
